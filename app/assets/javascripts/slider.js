   /*

    $(".average_score").each(function(index, value) {


    var average = parseFloat($(this).data("average"));

     $(".slider").each(function(index, value) {
    $(this).css("left", average * 20 + "px");

        //return false;
    });
       // return false;
});
       */